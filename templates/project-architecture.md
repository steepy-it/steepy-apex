# Project Architecture

> Durable hub document for cross-cutting decisions only. Init creates this document when a
> decision needs it and links it from `_INDEX.md`; a smaller, single-surface decision belongs in
> `conventions.md` or a surface standard instead.

## Cross-cutting decisions
(One entry per decision: what was decided and its scope across surfaces.)

## Reasons
(Why each decision was made — the reason travels with the decision.)

## Version policy
(The project's policy for choosing and bumping versions. The manifest and lockfile remain the
source of truth for resolved version numbers; this section never repeats them.)
